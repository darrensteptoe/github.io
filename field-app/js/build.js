// js/build.js
// Phase 11 — Build Identity Stamp
// Single source of truth for app version + build id.
// These values are display-only metadata and must not affect scenario math or hashing.

export const APP_VERSION = "1.0";   // UI version string (keep v1.0)
export const BUILD_ID = "dev";      // Manually edit before deploy (e.g., "2026-02-17a")
